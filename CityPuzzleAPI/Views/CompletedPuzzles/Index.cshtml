@model IEnumerable<CityPuzzleAPI.Model.CompletedPuzzle>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PuzzleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Score)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PuzzleId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Score)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CompletedPuzzleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CompletedPuzzleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CompletedPuzzleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
